<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- when checked (like is true) -->
    <item android:drawable="@drawable/red_heart" android:state_checked="true"/>

    <!-- default (like is false) -->
    <item android:drawable="@drawable/default_heart_btn"/>
</selector>
